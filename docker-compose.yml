services:
  api:
    build:
      context: .
    command:
      - uvicorn
      - apps.api.main:app
      - --host
      - 0.0.0.0
      - --port
      - "8000"
    environment:
      WATCHDOG_ENVIRONMENT: development
    ports:
      - "8000:8000"
